#include <iostream>
#include <vector>
#include <cmath>

int
main()
{
    int num;
    std::cin >> num;
    std::vector<int> vec(num, 0), answer(num, 0);
    
    for(int i = 0; i < num; ++i) {
        std::cin >> vec[i];
    }
    for (int i = 0; i < num; ++i) {

    }

    return 0;
}